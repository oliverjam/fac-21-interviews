// An anagram is the result of rearranging the letters of a word to produce a new word.

// Note: anagrams are case insensitive

// Complete the function to return true if the two arguments given are anagrams of each other; return false otherwise.

function isAnagram(string, original) {

}

// =====
// Uncomment these tests to check your solution
// =====

// test(isAnagram("foefet", "toffee"), true);
// test(isAnagram("Buckethead", "DeathCubeK"), true);
// test(isAnagram("ound", "round"), false);
// test(isAnagram("rumble", "bumble"), false);
// test(isAnagram("apple", "pale"), false);