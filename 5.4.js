// Write a function that converts RGB decimal values into the corresponding hexadecimal string. Valid decimal values for RGB are 0 - 255. Any values that fall out of that range must be rounded to the closest valid value.

// Note: Your answer should always be 6 characters long

function rgb(r, g, b) {
  
}

// =====
// Uncomment these tests to check your solution
// =====

// test(rgb(0, 0, 0), '000000')
// test(rgb(0, 0, -20), '000000')
// test(rgb(300, 255, 255), 'FFFFFF')
// test(rgb(173, 255, 47), 'ADFF2F')
