// ATM machines allow 4 or 6 digit PIN codes. PIN codes cannot contain anything but exactly 4 digits or exactly 6 digits.

// Write a function that, given a string, returns true if the string is a valid PIN, otherwise returns false.

function validPIN(pin) {

}

// =====
// Uncomment these tests to check your solution
// =====

// test(validPIN("1234"), true);
// test(validPIN("0000"), true);
// test(validPIN("123456"), true);
// test(validPIN("000000"), true);
// test(validPIN("123"), false);
// test(validPIN("12345"), false);
// test(validPIN("1.234"), false);
// test(validPIN("-12345"), false);