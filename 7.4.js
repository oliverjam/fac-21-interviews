// Write a function that takes an array of non-negative integers and strings and returns a new array with the strings filtered out

function filter(array) {

}

// =====
// Uncomment these tests to check your solution
// =====

// test(filter([1, 2, 'a', 'b']), [1, 2]);
// test(filter([1, 2, 'aasf', '1', '123', 123]), [1, 2, 123]);
// test(filter(["a", 1, 1, 1]), [1, 1, 1]);
// test(filter(["a", "b", "c"]), []);